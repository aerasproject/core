export interface User {
    id: string;
    name: string;
    email: string;
    role: string;
    document: string;
    profilePic: string | null;
    createdAt: string;
    updatedAt: string;
}